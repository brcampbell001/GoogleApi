{
  "name": "googleapi",
  "version": "1.0.0",
  "description": "* Node\r * Express\r * V8\r * RESTful API\r * GET\r * POST\r * req, res\r * route parameters\r * query parameters\r * Postman\r * body-parser",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "node-fetch": "^2.0.0"
  }
}
